const { query, rows } = require("./_db");
const num=v=>Number(v||0)||0,norm=v=>String(v||"").trim().toLowerCase();
function requireDashboardToken(req,res){const expected=process.env.DASHBOARD_TOKEN||process.env.ADMIN_TOKEN||"";if(!expected)return true;const token=req.query?.token||req.headers["x-dashboard-token"]||req.headers.authorization?.replace(/^Bearer\s+/i,"");if(token!==expected){res.status(401).json({ok:false,error:"Sai token dashboard"});return false}return true}
function parseItems(v){if(Array.isArray(v))return v;try{const x=JSON.parse(v||"[]");return Array.isArray(x)?x:[]}catch{return[]}}
function getRange(range,start,end){const now=new Date(),to=end?new Date(end):now,from=start?new Date(start):new Date(now),key=String(range||"today");if(!start){if(key==="week")from.setDate(now.getDate()-6);else if(key==="month"){from.setDate(1);from.setHours(0,0,0,0)}else if(key==="year"){from.setMonth(0);from.setDate(1);from.setHours(0,0,0,0)}else if(key==="all"){from.setFullYear(2020);from.setMonth(0);from.setDate(1);from.setHours(0,0,0,0)}else from.setHours(0,0,0,0)}return{key,from,to}}
function groupKey(date,mode){const d=new Date(date);if(mode==="month")return`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}`;if(mode==="year")return String(d.getFullYear());if(mode==="hour")return String(d.getHours()).padStart(2,"0")+":00";return d.toISOString().slice(0,10)}
function itemQty(i){return num(i.qty??i.quantity??i.count??1)||1}function itemSell(i){return num(i.price??i.sellPrice??i.salePrice??i.unitPrice??0)}function itemCost(i){return num(i.originalPrice??i.original_price??i.cost??i.capital??i.basePrice??0)}function itemName(i){return String(i.name||i.title||i.id||i.slug||"Món")}function itemKeys(i){return[i.id,i.slug,i.menuId,i.menu_id,i.name,i.title].map(norm).filter(Boolean)}
async function getMenuMap(){const r=await query("SELECT id,name,category,original_price,price FROM menu").catch(()=>({rows:[]}));const m=new Map();for(const x of rows(r)){const d={id:x.id,name:x.name,category:x.category,sell:num(x.price),cost:num(x.original_price)};[x.id,x.name].map(norm).filter(Boolean).forEach(k=>m.set(k,d))}return m}
function lookupMenu(i,m){for(const k of itemKeys(i))if(m.has(k))return m.get(k);return null}
function isDone(o){return["done","completed","complete","paid","success","finished","closed"].includes(norm(o.status))}function isCancelled(o){return["cancelled","canceled","cancel","deleted"].includes(norm(o.status))}
module.exports=async function handler(req,res){try{if(!requireDashboardToken(req,res))return;const range=getRange(req.query.range,req.query.start,req.query.end),groupBy=String(req.query.groupBy||"day"),menuMap=await getMenuMap();const orderResult=await query(`SELECT * FROM orders WHERE COALESCE(created_at,updated_at,now()) >= $1 AND COALESCE(created_at,updated_at,now()) <= $2 ORDER BY COALESCE(created_at,updated_at,now()) ASC`,[range.from.toISOString(),range.to.toISOString()]).catch(()=>({rows:[]}));const bookingResult=await query(`SELECT * FROM bookings WHERE COALESCE(created_at,updated_at,now()) >= $1 AND COALESCE(created_at,updated_at,now()) <= $2`,[range.from.toISOString(),range.to.toISOString()]).catch(()=>({rows:[]}));const inventoryResult=await query("SELECT * FROM inventory ORDER BY name ASC").catch(()=>({rows:[]}));const orders=rows(orderResult),bookings=rows(bookingResult),inventory=rows(inventoryResult),completed=orders.filter(isDone),cancelled=orders.filter(isCancelled);let revenue=0,cost=0,profit=0,productsSold=0;const seriesMap=new Map(),hourMap=new Map(),itemMap=new Map(),tableMap=new Map(),customers=new Set();
for(const order of completed){const created=new Date(order.created_at||order.updated_at||Date.now()),tableId=String(order.table_id||order.table||order.tableId||"Mang về"),phone=String(order.phone||order.customer_phone||order.customer?.phone||"").trim();if(phone)customers.add(phone);const items=parseItems(order.items||order.menu_items||order.order_items);let orderRevenue=0,orderCost=0,orderQty=0;for(const item of items){const menu=lookupMenu(item,menuMap),quantity=itemQty(item),price=itemSell(item)||num(menu?.sell),original=itemCost(item)||num(menu?.cost),lineRevenue=price*quantity,lineCost=original*quantity,lineProfit=lineRevenue-lineCost;orderRevenue+=lineRevenue;orderCost+=lineCost;orderQty+=quantity;productsSold+=quantity;const name=itemName(item),row=itemMap.get(name)||{name,category:menu?.category||item.category||"",quantity:0,revenue:0,cost:0,profit:0,marginPercent:0};row.quantity+=quantity;row.revenue+=lineRevenue;row.cost+=lineCost;row.profit+=lineProfit;row.marginPercent=row.revenue?row.profit/row.revenue*100:0;itemMap.set(name,row)}if(!orderRevenue)orderRevenue=num(order.total||order.amount||order.price);if(!orderCost)orderCost=num(order.cost||order.original_price||order.capital);const orderProfit=orderRevenue-orderCost;revenue+=orderRevenue;cost+=orderCost;profit+=orderProfit;const g=groupKey(created,groupBy),s=seriesMap.get(g)||{label:g,revenue:0,cost:0,profit:0,orders:0,items:0};s.revenue+=orderRevenue;s.cost+=orderCost;s.profit+=orderProfit;s.orders+=1;s.items+=orderQty;seriesMap.set(g,s);const h=groupKey(created,"hour"),hr=hourMap.get(h)||{label:h,revenue:0,orders:0};hr.revenue+=orderRevenue;hr.orders+=1;hourMap.set(h,hr);const t=tableMap.get(tableId)||{table:tableId,revenue:0,cost:0,profit:0,orders:0,items:0};t.revenue+=orderRevenue;t.cost+=orderCost;t.profit+=orderProfit;t.orders+=1;t.items+=orderQty;tableMap.set(tableId,t)}
const lowStock=inventory.map(x=>({id:x.id,name:x.name||x.item_name||x.title||"Hàng",quantity:num(x.quantity||x.qty||x.stock),minQuantity:num(x.min_quantity||x.minQty||x.warning_quantity),unit:x.unit||""})).filter(x=>x.minQuantity>0&&x.quantity<=x.minQuantity).sort((a,b)=>a.quantity-b.quantity);return res.status(200).json({ok:true,range:{key:range.key,from:range.from.toISOString(),to:range.to.toISOString(),groupBy},summary:{revenue,cost,profit,marginPercent:revenue?profit/revenue*100:0,completedOrders:completed.length,cancelledOrders:cancelled.length,totalOrders:orders.length+bookings.length,shipOrders:orders.length,tableOrders:bookings.length,productsSold,customers:customers.size,avgOrder:completed.length?revenue/completed.length:0,lowStockCount:lowStock.length},series:[...seriesMap.values()],hourly:[...hourMap.values()].sort((a,b)=>a.label.localeCompare(b.label)),items:[...itemMap.values()].sort((a,b)=>b.revenue-a.revenue),tables:[...tableMap.values()].sort((a,b)=>b.revenue-a.revenue),inventory:{totalItems:inventory.length,lowStock}})}catch(e){console.error("dashboard-analytics error:",e);return res.status(500).json({ok:false,error:e.message})}};
